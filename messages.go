package cmdbox

// Keep all messages in this file so they can be overriden by composite
// authors depending on the locale.

var Messages = map[string]string{
	"InvalidName": "invalid name (lower case words only)",
}
