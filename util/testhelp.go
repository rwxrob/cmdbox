package util

const gotwanted = `
	wanted: %v
	got:    %v
`

/*
// GotWanted does basic testing of wanted
func GotWanted(t *testing.T, tests [][]interface{}) bool {
	for n, test := range tests {
		_n += 1
		switch len(tests) {
		case 0:
			t.Log("empty test: " + _n)
			continue
		case 1:
		case 2:
		default:
		}
	}
	return false
}
*/
